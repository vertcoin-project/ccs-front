email: vertion@protonmail.com
name: Vertcoin
description: Vertcoin is a digital currency.
keywords: "vertcoin, vtc, cryptocurrency"
baseurl: ""
url: "https://vertcoin.org"

markdown: kramdown

# Kramdown was using smart quotes, which messes up CLI examples
# TODO: smart quotes are actually quite pretty, so this is perhaps better handled via a plugin that reverts them for CLI blocks
kramdown:
    smart_quotes: ["apos", "apos", "quot", "quot"]
    input: GFM

exclude: ["README.md"]